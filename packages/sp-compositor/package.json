{
  "name": "sp-compositor",
  "version": "0.0.2",
  "description": "",
  "main": "dist/sp-compositor.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "run/dev.sh",
    "build": "babel -d dist src",
    "prepublish": "npm run build"
  },
  "author": "",
  "chromium-args": "--no-sandbox --disable-gpu-sandbox --enable-webgl --ignore-gpu-blacklist --enable-logging=stderr",
  "window": {
    "title": "Streamplace Compositor",
    "width": 1920,
    "height": 1080,
    "min_width": 1920,
    "min_height": 1080,
    "resizable": true,
    "frame": true
  },
  "license": "Apache-2.0",
  "devDependencies": {
    "babel-preset-streamplace": "0.0.1"
  }
}
