// Color scheme stub. The app's useColorScheme returns "dark" on web
// until the real light/dark toggle lands; we mirror that here.
// Phase 7 polish is when this gets a real implementation.
export function useColorScheme(): "dark" | "light" {
  return "dark";
}
