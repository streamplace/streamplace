{
  "name": "sk-schema",
  "version": "0.0.1",
  "description": "Schema for Stream Kitchen",
  "main": "dist/schema.json",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "edit": "swagger project edit -s -p 4949 --host 0.0.0.0",
    "build": "mkdir -p dist && js-yaml api/swagger/swagger.yaml > dist/schema.json",
    "dev": "nodemon -w api -x npm run build"
  },
  "author": "",
  "license": "Apache-2.0",
  "devDependencies": {
    "js-yaml": "^3.5.2",
    "nodemon": "^1.9.1",
    "swagger": "^0.7.5"
  }
}
