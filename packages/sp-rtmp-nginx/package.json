{
  "name": "sp-rtmp-nginx",
  "version": "0.1.3",
  "description": "Streamplace RTMP Server",
  "main": "package.json",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "! which docker && exit 0 || docker build -f build.Dockerfile -t nginx-rtmp-build . && docker run --rm nginx-rtmp-build cat /usr/local/nginx/sbin/nginx > nginx && chmod 755 nginx"
  },
  "repository": {
    "type": "git",
    "url": "git+ssh://git@github.com/streamplace/streamplace.git"
  },
  "author": "Streamplace",
  "license": "Apache-2.0",
  "bugs": {
    "url": "https://github.com/streamplace/streamplace/issues"
  },
  "homepage": "https://github.com/streamplace/streamplace#readme",
  "dependencies": {
    "sp-node": "^0.1.3"
  }
}
