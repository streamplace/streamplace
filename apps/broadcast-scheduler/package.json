{
  "name": "broadcast-scheduler",
  "version": "0.0.0",
  "description": "Create the appropriate vertices and arcs for a broadcast.",
  "main": "dist/broadcast-scheduler.js",
  "scripts": {
    "build": "babel -d dist src",
    "dev": "nodemon -w src -w ../sk-client/dist -w ../sk-config/dist -x babel-node src/broadcast-scheduler.js"
  },
  "author": "Stream Kitchen LLC",
  "license": "Apache-2.0",
  "dependencies": {
    "underscore": "^1.8.3",
    "winston": "^2.2.0"
  },
  "devDependencies": {
    "babel-cli": "^6.10.1",
    "babel-core": "^6.9.1",
    "babel-preset-streamkitchen": "^0.0.1"
  }
}
