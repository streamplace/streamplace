/**
 * Shared behavior of all inputs. This does a little too much right now... eventually the No
 */
import BaseVertex from "./BaseVertex";

export default class InputVertex extends BaseVertex {
  constructor(params) {
    super(params);
  }
}
