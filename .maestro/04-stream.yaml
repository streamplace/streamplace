appId: tv.aquareum
---
- launchApp
- extendedWaitUntil:
    visible:
      text: "${ACCOUNT_HANDLE}"
    timeout: 30000
- tapOn:
    text: "${ACCOUNT_HANDLE}"
- extendedWaitUntil:
    visible:
      text: "LIVE"
    timeout: 10000
