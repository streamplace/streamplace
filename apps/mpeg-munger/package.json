{
  "name": "mpeg-munger",
  "version": "0.0.0",
  "description": "Transform the PTS and DTS of an mpeg-ts stream",
  "main": "dist/MpegMungerStream.js",
  "scripts": {
    "build": "babel -d dist src",
    "dev": "nodemon -w src -x npm run build",
    "test": "mocha --compilers js:babel-register"
  },
  "author": "Stream Kitchen",
  "license": "Apache-2.0",
  "devDependencies": {
    "babel-cli": "^6.7.5",
    "babel-core": "^6.7.6",
    "babel-preset-streamkitchen": "^0.0.1",
    "expect.js": "^0.3.1",
    "mocha": "^2.4.5",
    "nodemon": "^1.9.1"
  }
}
